/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        api_base_url:"https://denaro-node.stackschools.com" //"https://node-forwarder.denaro.is" //"http://127.0.0.1:3002"
    }    
};

export default nextConfig;
